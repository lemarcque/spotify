<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.capsulo.spotify">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_spotify_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name="io.capsulo.spotify.features.loader.LoaderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="io.capsulo.spotify.features.player.PlayerActivity" />
    </application>
</manifest>
